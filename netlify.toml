[build]
  command = "node scripts/prepare-chat-function.mjs && npm run build"
  publish = "dist/client"

  [build.environment]
    NODE_VERSION = "22"

  [build.processing]
    ignore_html_forms = false

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888
